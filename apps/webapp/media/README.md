# Files served at /media/ on the site.
#
# Not in git: the report's recording is tens of megabytes, and a repository is
# the wrong place for it. It has to be present on whatever machine builds the
# image — see infra/webapp.Dockerfile, which copies the working tree.
